<!DOCTYPE html>
<html lang="en">
<head>
	<title>moviebomber.org | <%-username%> | Inbox</title>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="robots" content="noindex,nofollow">
	<% include ./styles %>
	<% include ./fonts %>
</head>
<body>
	<% include ./nav %>
	<main>
		<div class="top-wrapper row">
			<div class="content-wrapper">
				<div class="col s12">
					<div class="row profile-header mb-0">
						<div class="col s6 mt-1">
							<h5 id="username" class="cyan-text">
								{ <span class="grey-text">Inbox</span> }
							</h5>
						</div>
                        <div class="col s12 mt-2 profile-action-buttons">
                            <span class="grey-text">Messages</span>
                        </div>
                    </div>
                    
					<% if (typeof inbox != 'undefined') { %>
						<% inbox.forEach(m => { %>
							<div class="row valign-wrapper line mb-0">
								<div class="col s8 m2">
									<a class="point" href="/@/<%-m.friendUsername%>" data-sender="<%-m.friendId%>"><%-m.friendUsername%></a>
								</div>
								<div class="col s6 hide-on-small-only">
									<a href="/account/inbox/<%-m.sid%>" class="grey-text point"><%-`${m.messagePreview}..`%></a>
								</div>
								<div class="col s2 hide-on-med-and-up">
									<a href="/account/inbox/<%-m.sid%>" class="grey-text point">View</a>
								</div>
								<div class="col s2">
									<% if (m.new == false) { %>
										<a><i class="material-icons grey-text">update</i></a>
									<% } else { %>
										<a class="valign-wrapper">
											<i class="material-icons correct">fiber_new</i>
										</a>
									<% } %>
								</div>
								<div class="col s2">
									<a class="point valign-wrapper">
										<i id="<%-m.sid%>" class="material-icons red-text delete">delete</i>
									</a>
								</div>
							</div>
						<% }); %>
					<% } %>
                </div>
			</div>
		</div>
	</main>

	<% include ./footer %>

	<script>
		let socketString = <%-JSON.stringify(socket)%>;
		let usernameString = <%-JSON.stringify(username)%>;
		let userIdString = <%-JSON.stringify(userId)%>;
	</script>
	<script src="/js/jquery.slim.min.js"></script>
	<script src="<%-socket%>/socket.io/socket.io.js"></script>
	<script src="/js/materialize.min.js"></script>
	<script src="/js/init.js"></script>
	<script src="/js/nav.js"></script>
	<script src="/js/inbox.js"></script>
	<script src="/js/sweetalert.min.js"></script>
</body>
</html>